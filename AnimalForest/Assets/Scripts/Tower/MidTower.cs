using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MidTower : Tower
{
    // Start is called before the first frame update
    void Start()
    {
        hp = 800;
        max_hp = 800;
    }
}